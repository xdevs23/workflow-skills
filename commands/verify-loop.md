---
description: Adversarially verify every claim in a target artifact against ground truth until immaculate.
---

Invoke the `verify-loop` skill against the target below. Establish the target artifact and ground truth
if not obvious, then run the verify → reconcile → research → fix → re-verify loop to convergence.

$ARGUMENTS
