---
description: Implement a code change, then run adversarial reviewers and a verify+fix pass before committing.
---

Invoke the `implement-review-verify` skill for the change below. Establish the change scope and
acceptance criteria if not obvious, then run implement → review → verify+fix.

$ARGUMENTS
