---
description: Audit an artifact for sins of omission — what should be present but is missing or under-specified.
---

Invoke the `find-gaps` skill against the target below. Establish the artifact and its intended scope if
not obvious, then run the gap-finding pass and return a severity-graded list.

$ARGUMENTS
